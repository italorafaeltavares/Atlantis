version: 3

automerge: true

projects:
  - name: main
    dir: .
    autoplan:
      enabled: true
      when_modified: ["*.hcl", "*.tf", "*.tfvars"]
    workflow: my-workflow

workflows:
  my-workflow:
    apply:
      steps:
        - run: terragrunt apply
