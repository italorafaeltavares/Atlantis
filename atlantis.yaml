version: 3

automerge: true
projects:
  - name: main
    dir: .
    autoplan:
      enabled: true
      when_modified: ["*.tf", "*.tfvars"]
    workflow: terraform-workflow

workflows:
  terraform-workflow:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply

