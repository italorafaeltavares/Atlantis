version: 3

automerge: true

projects:
  - name: main
    dir: .
    autoplan:
      enabled: true
      when_modified: ["*.hcl", "*.tf", "*.tfvars"]
    workflow:
      apply:
        steps:
          - run: terragrunt apply
